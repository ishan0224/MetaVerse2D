export {
  getClientPlayerId,
  getRoomId,
  getSocketClient,
  getWorldId,
  listenToPlayerUpdates,
  onWebRTCAnswer,
  onWebRTCIceCandidate,
  onWebRTCOffer,
  sendInput,
  sendWebRTCAnswer,
  sendWebRTCIceCandidate,
  sendWebRTCOffer,
  setPlayerAvatarId,
  setPlayerAvatarUrl,
  setPlayerName,
  setRoomId,
  setWorldId,
} from '@/network/socket/socketClient';
