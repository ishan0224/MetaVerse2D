# Config Package

Shared constants and configuration contracts consumed across workspaces.

Use this package for:
- Environment key names and defaults.
- Cross-app constants that must stay in sync.
- Framework-agnostic config utilities.
