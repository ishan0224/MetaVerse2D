# Shared Package

Cross-workspace TypeScript contracts and shared interfaces.

Use this package for:
- Player, room, and transport event types.
- Shared DTOs and validation-ready shapes.
- Contracts consumed by both frontend and backend.

Current contract note:
- `Player` includes identity fields (`id`, `name`) and authoritative state (`x`, `y`, etc.).
