# Shared Types

Canonical cross-app TypeScript types and interfaces.

Guidelines:
- Keep types framework-agnostic.
- Avoid business behavior in type modules.
- Version contracts deliberately to prevent runtime drift.
